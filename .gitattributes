# Auto detect text files and perform LF normalization
* text=auto

# Fonts — binär, keine Zeilenenden-/Diff-Behandlung
*.woff2 binary
*.woff  binary
*.ttf   binary
*.otf   binary

# Bilder — binär
*.jpg   binary
*.jpeg  binary
*.png   binary
